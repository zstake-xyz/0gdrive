import UploadCardContainerNew from '@/components/upload/UploadCardContainer';

/**
 * Re-export the UploadCardContainer from the upload directory
 * This is kept for backward compatibility with existing imports
 */
export default UploadCardContainerNew; 